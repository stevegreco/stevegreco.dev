---
interface Props {
  slug: string
  external?: boolean
}

const { slug, external = false } = Astro.props
---

<a
  class="text-foreground text-sm lg:text-base inline-block"
  href={slug}
  {...external && { target: '_blank', rel: 'noopener noreferrer' }}
>
  <slot />
</a>
